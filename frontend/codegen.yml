schema: ${API_URL}
documents: ./src/**/*.graphql
generates:
  ./src/types.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql
    config:
      withHooks: true
      withComponent: false
      withHOC: false
