subscription GameState($sessionId: ID!) {
  playGame(sessionId: $sessionId) {
    status
    currentQuestion {
      id
      text
      ui
      answers {
        id
        text
      }
    }
    leaderboard {
      name
      score
      color
    }
  }
}
